# Postgres connection for the dashboard. Point it at the database your
# PgWorkflows workers use. The compose stack in the repo root exposes it here:
DATABASE_URL=postgres://postgres:postgres@localhost:55432/pgworkflows
